cff-version: 1.2.0
message: "If you use MapSmith in your work, please cite it as below."
title: "MapSmith: geospatial processing for AI agents, with verifiable provenance"
type: software
authors:
  - name: "The MapSmith project"
repository-code: "https://github.com/mapsmith-ai/MapSmith"
url: "https://mapsmith.dev"
doi: "10.5281/zenodo.22213091"
identifiers:
  - type: doi
    value: "10.5281/zenodo.22213091"
    description: "Concept DOI — always resolves to the latest release"
license: AGPL-3.0-or-later
version: "0.4.0"
date-released: "2026-08-31"
keywords:
  - GIS
  - geospatial
  - provenance
  - reproducibility
  - Model Context Protocol
  - MCP
abstract: >-
  An MCP server that gives AI agents professional geoprocessing under a rule
  the agent cannot bend: every geometry and every number comes from a real
  engine run, never from the model. Each operation that writes a dataset also
  writes a manifest beside it — the inputs and their hashes, the exact
  parameters, the coordinate-system decisions and the reason for each, the
  engine and its version, and the deterministic checks that ran — and the
  manifest is written before a failed check raises, so the audit trail survives
  the error. Capabilities are reached through a searchable catalogue rather
  than a wall of tools, because a caller's ability to pick the right tool
  degrades long before a catalogue does. A manifest records what was done; it
  does not certify that what was done was right, and MapSmith says so in its
  own documentation rather than leaving a reader to find out.
