{
  "upload_type": "software",
  "title": "MapSmith: geospatial processing for AI agents, with verifiable provenance",
  "version": "0.4.0",
  "creators": [
    {"name": "The MapSmith project"}
  ],
  "license": "agpl-3.0-or-later",
  "access_right": "open",
  "keywords": [
    "GIS",
    "geospatial",
    "provenance",
    "reproducibility",
    "Model Context Protocol",
    "MCP"
  ],
  "description": "<p>An MCP server that gives AI agents professional geoprocessing under a rule the agent cannot bend: every geometry and every number comes from a real engine run, never from the model.</p><p>Each operation that writes a dataset also writes a manifest beside it &mdash; the inputs and their hashes, the exact parameters, the coordinate-system decisions and the reason for each, the engine and its version, the environment that changed the answer, and the deterministic checks that ran. The manifest is written <em>before</em> a failed check raises, so the audit trail survives the error rather than disappearing with it.</p><p>Capabilities are reached through a searchable catalogue rather than a wall of tools, because a caller's ability to pick the right tool degrades long before a catalogue does. The catalogue answers with a set of candidates and the text that distinguishes them, not with a verdict.</p><p>A manifest records what was done; it does <em>not</em> certify that what was done was right. MapSmith says so in its own documentation, and a separate correctness suite exists to measure the difference.</p>"
}
